# Dependencies
node_modules/

# Logs
*.log
logs/

# Environment & secrets
.env
.env.*
!.env.example

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp

# Build output is COMMITTED (dist/index.js is the package entry that cordis
# loads under node_modules, where Node refuses to strip TypeScript types).
# Regenerate with `pnpm build` after changing src/.
